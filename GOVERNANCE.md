# nlsqlc governance

The maintainer is responsible for security review, API compatibility, and release decisions. Public changes require reproducible build and test evidence. The Query IR and public C API are versioned compatibility surfaces.
